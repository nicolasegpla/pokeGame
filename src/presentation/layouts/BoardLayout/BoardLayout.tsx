import './board-layout.scss';

interface BoardLayoutProps {
    // Agregá tus props acá
}

export function BoardLayout({}: BoardLayoutProps) {
    return (
        <div className="board-layout">
            <h2>BoardLayout works!</h2>
        </div>
    );
}
